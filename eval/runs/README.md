# Eval runs

Phase 3B runner outputs belong here under an `eval_run_id`. A run manifest must
record planner, prompt, model and fixture-set versions. Generated run artifacts
should not be treated as Golden Case definitions.
